# PDF CLIENTE Replacer

Ứng dụng Streamlit thay thế khối thông tin **CLIENTE** trong file PDF, giữ nguyên bố cục và font gần giống bản gốc.

## 🚀 Cách chạy local
```bash
pip install -r requirements.txt
streamlit run app.py
